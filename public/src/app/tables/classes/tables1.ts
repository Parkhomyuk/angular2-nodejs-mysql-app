export class Users{
  id:number;
  fullname: string;
  profile: string;


 /* constructor(id:number, fullname:string, profile:string) {
      this.id = id;
      this.fullname = fullname;
      this.profile = profile;
      }*/
  constructor(id:number, fullname:string, profile:string) {
    this.id = id;
    this.fullname = fullname;
    this.profile = profile;
  }

}
