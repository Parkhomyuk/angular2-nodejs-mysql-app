export class Hiro{
id: number;
name: string;
profile:string;


  constructor(id:number, name:string, profile:string) {
      this.id = id;
      this.name = name;
      this.profile = profile;
      }
}
